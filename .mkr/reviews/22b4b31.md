# Code review — 514e763 + 22b4b31 (fix(ci): mkr-gate.yml sources EXPECTED_PRIOR_TIP from pull_request.base.sha too)

**Reviewers.** Two independent fresh agents, `mkr-code-reviewer` and `mkr-security-reviewer`,
spawned in parallel against this commit's diff, neither aware of the other's findings until each
had formed its own judgment.

mkr-code-reviewer: READY
mkr-security-reviewer: READY

**Scope.** `.github/workflows/mkr-gate.yml` (`EXPECTED_PRIOR_TIP` sourcing widened to also cover
`pull_request` events, per specs/ReviewRecordPullRequestPriorTip_Spec.md §6), `.claude/hooks/lib/reviewrecord.sh`
(comment-only update naming the new source), `docs/adr/0011-review-record-pull-request-prior-tip.md`
(new ADR), `tests/mkr_artifact_test.sh` (`TC-GATE-01..04`).

## Findings

None blocking from either reviewer.

Non-blocking, actioned in this same review round:
- `mkr-code-reviewer`: `TC-GATE-01`'s block redundantly reassigned `GATE_YML_FILE`, already set
  above for `TC-RRF-16` and still in scope. Fixed (commit `22b4b31`) — comment left in its place,
  no behavior change, full suite re-confirmed green.

Non-blocking, not actioned (pre-existing, independent of this diff, per the reviewer's own
framing):
- `mkr-security-reviewer`: constructed a concrete exploit attempt (a crafted fork-PR merge commit
  whose first parent equals `main`'s real tip) against the newly-enabled AD-2/AD-3 shortcut on
  `pull_request` events, and confirmed the existing tree-equality gate (`git merge-tree
  --write-tree`) holds — no new content can be smuggled through. Separately, while adversarially
  probing `find_review_record` end-to-end, confirmed a genuinely pre-existing gap unrelated to this
  diff: the bounded non-code-chain fallback's exact-match check never consults
  `expected_prior_tip` at all, so a trailing commit self-naming a fabricated `VERDICT: READY`
  record already fully bypasses the function on `pull_request` events today, with or without this
  fix — already documented in `reviewrecord.sh`'s own comments as an accepted residual risk
  resting on human merge-review, not this CI check. Worth a dedicated future spec re-examining
  that risk now that `pull_request` checks are more load-bearing per this fix's own Intent, but
  explicitly not a defect of this diff — reachability and severity are unchanged by it.

## Verification discipline

Both reviewers hand-traced `TC-GATE-01` through `TC-GATE-04` against the real, current committed
files (not the commit message's own claims) — confirming each test's grep target actually exists
at the cited line and actually distinguishes the pre-fix state from the post-fix one.
`mkr-code-reviewer` specifically re-verified `TC-GATE-02`'s own test-first bug (a naive single-line
grep for a phrase that spans two physical lines in the real YAML, silently passing for the wrong
reason even before the fix) was genuinely fixed, not just claimed fixed. `mkr-security-reviewer`
additionally re-verified `EXPECTED_PRIOR_TIP`/`TARGET_SHA` are passed via `env:` and referenced as
quoted shell variables, never interpolated via `${{ }}` into the `run:` script body — no
GitHub-Actions script-injection surface, unchanged by this diff.

Full suite (`config_test.sh` 120/120, `mkr_artifact_test.sh` 249/249, `hooks_test.sh` 180/180,
`install_test.sh` 74/74) green before this record.

## Verdict

VERDICT: READY
