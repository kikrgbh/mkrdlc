# Code review — 6736343 (WorktreeGuard_Spec rev 6→11 + ADR follow-up, fixing the phase-9 audit finding)

**Reviewers.** Two independent fresh agents, spawned in parallel against the diff at `80599fe`
(rev 6→11 of `specs/WorktreeGuard_Spec.md` plus `docs/adr/0012-worktree-guard-policy-tiers.md`,
against merge-base `e61fd10` — the commit already merged as PR #27), neither aware of the other's
findings until each had formed its own judgment.

mkr-code-reviewer: READY
mkr-security-reviewer: READY

**Scope.** `specs/WorktreeGuard_Spec.md` (rev 6 → rev 11) and `docs/adr/0012-worktree-guard-policy-tiers.md`,
against merge-base `e61fd10`. No guard source touched. This follow-up fixes a phase-9
grounding-audit finding (`.mkr/audits/e61fd10.md`, `AC1 NOT VERIFIED`): the spec undersold
`procwalk_statement_has_git_keyword`'s "ambiguous fallback" (`procwalk.sh:398-404`, regex character
class `[-$]`) as matching only literal `commit`/`checkout`/`switch` keyword occurrences, when it
also matches any `git` invocation immediately followed by a flag-shaped token *or* a bare
`$`-prefixed token, regardless of actual subcommand.

Getting this right took 5 G1 review rounds (rev 7–11) — two recurring defect classes, each caught
three times: an incomplete sweep (a fix landing in some locations describing the mechanism but
missing others) and an audit-trail drift (a `§9` test-case-register row's own status text going
stale relative to the document's real revision history). Both `mkr-code-reviewer` and
`mkr-security-reviewer` independently re-verified the core technical claim against
`procwalk.sh:398-404` directly — not the document's account of it — and confirmed it is now accurate
and consistent everywhere in the document that describes it.

**Human override (disclosed, not re-litigated by this review).** After the core technical finding
was independently confirmed correct across 3 consecutive rounds, `kikrgbh` explicitly stopped the
G1 revision loop rather than continuing to chase the review process's own diminishing returns. Two
secondary findings from the final G1 round were deliberately left disclosed-but-unresolved rather
than fixed: (1) whether `AC2`/`TC-WGSPEC-06`'s message-clarity certification (from rev 2's G3 round)
still holds now that a denial can be triggered by the ambiguous-fallback match rather than a real
commit/checkout/switch — stated as an open, disputed question in `§11` DoD bullets 2–3 and
`TC-WGSPEC-06`'s row, not asserted either way; (2) the ADR's Context section is a stale summary of
the spec's real acceptance history — a disclosure note was added pointing to the spec's `§13`
instead of rewriting that section. Both reviewers were instructed not to re-flag these unless the
disclosure itself was missing or misleading; neither found it was.

## Finding 1 — non-blocking, confirmed — ADR Consequences bullet lacked the AC2 caveat

`mkr-security-reviewer`: the ADR's Consequences section stated AC2's message-clarity certification
as settled with no caveat, while the spec's own current text explicitly discloses this as open —
a reader of the ADR alone (without cross-checking the spec) would miss that. **Fixed** in a
follow-up commit (`6736343`) — the bullet now states it was confirmed "against the message text as
it stood at rev 2" and points to the spec's `§11`/`TC-WGSPEC-06` for the current, disputed status.

## Findings not pursued further

`mkr-code-reviewer`'s two non-blocking observations (the `.mkr/audits/e61fd10.md` citation pointing
to a file not present in this worktree — already flagged at rev 7 and verified against source
directly instead; and one DoD bullet's vague forward-reference phrasing) — neither affects any
acceptance criterion's substance; left as-is per the same "stop the loop" decision the human already
made for the two larger disclosed items.

## Verification discipline

Both reviewers independently re-derived the `[-$]` regex from `procwalk.sh:398-404` directly and
traced it through the real call graph (`procwalk_resolve_target_dir(s)`, both guard scripts'
`deny()` paths) rather than accepting the document's narrative. `mkr-code-reviewer` additionally
grepped every "flag" occurrence across both files to confirm no surviving instance of the
incomplete-sweep defect class, and cross-checked `§1`/`§11`/`§13` against each other for drift.

## Verdict

VERDICT: READY
