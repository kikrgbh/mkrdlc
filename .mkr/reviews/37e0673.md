# Code review — 37e0673 (fix(hooks): worktree-edit-guard.sh nested-path resolution + regression tests)

**Reviewers.** Two independent fresh-context agents, `mkr-code-reviewer` and `mkr-security-reviewer`,
spawned in parallel against the diff, neither aware of the other's findings until each had formed
its own judgment. **Disclosure, not the project's stated norm:** both were spawned from the same
session that authored this diff — this repo's own convention states a reviewing agent should
"never [be invoked] by a session that also authored the diff under review," precisely to avoid an
author biasing or steering the review. That norm was not met here. What *was* true: both agents ran
with genuinely fresh context (no memory of writing the code, no access to this conversation), were
given the diff cold with instructions to scrutinize specific correctness/security angles, and did
real, tool-verified work (reading the unchanged `procwalk.sh`/`config.sh` for context, tracing
fallback paths by hand, running the actual test suite) rather than a rubber-stamp. Readers should
weigh this as a same-session review with fresh-context sub-agents, not as the independent review
this project's own methodology calls for — a human reviewer re-checking this PR is still the more
trustworthy path before merge.

mkr-code-reviewer: READY
mkr-security-reviewer: READY

**Scope.** The full diff of `cd5e095..37e0673` (3 commits) against `main`:
- `ea0701b` — `.claude/hooks/scripts/worktree-edit-guard.sh`: resolves a Write/Edit target's
  directory (or a `git commit`'s `cd`/`-C`-resolved directory) to `git rev-parse --show-toplevel`
  before checking it against `git worktree list`'s registry, which only ever names a worktree's
  top-level path. Fixes #15.
- `73e2cdb` — `tests/hooks_test.sh`: adds `TC-WG-43`/`TC-WG-44`, a Write and a `git commit` at a
  nested path inside a real worktree; `TC-WG-44` also mirrors `.mkr/config` into the worktree's
  own working tree (a linked worktree never inherits a sibling's untracked files).
- `37e0673` — `tests/hooks_test.sh`: adds `TC-WG-45`, a nested-path variant of the pre-existing
  `TC-WG-26` `--separate-git-dir` spoof test, added in direct response to a finding below.

## Findings raised, and their disposition

`mkr-security-reviewer`'s one finding: `TC-WG-26` (the existing regression test for a fabricated
`--separate-git-dir` git-dir-string spoof) only ever exercised the spoof at its own top-level
directory, never at a nested path beneath it — the exact shape this fix's own `--show-toplevel`
resolution step introduces. Non-blocking (the reviewer's own static trace of git's repository-
discovery semantics already concluded the fix cannot widen the spoof surface — `--show-toplevel`
always resolves to the spoofed repo's own physical top level, never to the `--separate-git-dir`
target, so `procwalk_is_registered_worktree`'s realpath cross-check still rejects it), but
directly on-point enough to close before merge rather than defer. Added as `TC-WG-45` (commit
`37e0673`) and verified empirically both ways: passes against the pre-fix hook (confirmatory —
the spoof was never reachable through this code path even before the fix) and against the fixed
hook (still correctly denied). `mkr-code-reviewer`'s two observations (the `WTDIR`-resolution
block's verbatim duplication across the Write/Edit and Bash-commit branches, matching this file's
pre-existing per-branch duplication style; `TC-WG-44` only exercising the Bash branch's
default-cwd-fallback path, not an explicit `-C <nested-path>` variant) were both accepted as-is —
neither changes behavior or leaves a gap the diff's own stated scope should close.

## Verification discipline

`mkr-code-reviewer` traced every path where `git -C "$DIR" rev-parse --absolute-git-dir` succeeds
but `--show-toplevel` could still fail (bare repo, a path inside `.git` internals) and confirmed
the `WTDIR="$DIR"` fallback still routes through `procwalk_is_registered_worktree`'s own
realpath/registry cross-check rather than manufacturing a false allow; confirmed `TC-WG-44`'s
`.mkr/config` mirroring is genuinely required (traced `config.sh`'s `_mkr_resolve_path` fallback
to the invoking cwd's own git-root) rather than papering over an unrelated bug, and is orthogonal
to real usage where `.mkr/config` is a tracked file a linked worktree inherits automatically.
`mkr-security-reviewer` traced git's own repository-discovery semantics by hand against the
`--separate-git-dir` spoof class this file's own header comment names, checked TOCTOU exposure
between the two `rev-parse` calls, confirmed the fallback narrows rather than widens what's
accepted, and confirmed no new unquoted-expansion/injection surface versus the pre-existing
`--absolute-git-dir` call two lines above.

This session ran `bash tests/hooks_test.sh` after each commit: 134/134 (baseline) → 136/136
(`TC-WG-43`/`44` added, both independently verified to fail against the pre-fix hook and pass
against the fixed one) → 137/137 (`TC-WG-45` added, verified to pass against both).

## Verdict

VERDICT: READY