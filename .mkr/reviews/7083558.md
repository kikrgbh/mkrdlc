# Code review — 96cae58 + 7083558 (fix(hooks): find_review_record terminates on a commit provably at-or-behind expected_prior_tip)

**Reviewers.** Two independent fresh agents, `mkr-code-reviewer` and `mkr-security-reviewer`,
spawned in parallel against this commit's diff, neither aware of the other's findings until each
had formed its own judgment.

mkr-code-reviewer: READY
mkr-security-reviewer: READY

**Scope.** `.claude/hooks/lib/reviewrecord.sh` (new ancestor-check in `find_review_record`,
per specs/ReviewRecordMergeMidChainFallback_Spec.md §6), `docs/adr/0008-review-record-bounded-chain-fallback.md`
(Consequences cross-reference to the newly-closed gap), `docs/adr/0010-review-record-merge-mid-chain-fallback.md`
(new ADR), `tests/hooks_test.sh` (TC-RRF-21..26), `tests/mkr_artifact_test.sh` (TC-RRF-27..28).

## Findings

None blocking from either reviewer.

Non-blocking, actioned in this same review round:
- `mkr-security-reviewer`: `git merge-base --is-ancestor` lacked the `--` end-of-options guard this
  file's own stated convention uses elsewhere for `<sha>`-derived git calls. Not exploitable (both
  arguments are always trusted 40-hex SHAs, never free text), but fixed for consistency (commit
  `7083558`), verified `git merge-base --is-ancestor --` accepts the guard correctly and the full
  suite stays green.

Non-blocking, not actioned (left for a future touch, per each reviewer's own framing):
- `mkr-code-reviewer`: §3's literal wording called for "a structural check... mirroring TC-RRF-14's
  role" in `tests/mkr_artifact_test.sh` for "no new config.sh key" specifically; the existing,
  unmodified `TC-RRF-14` (a whole-file grep over every `mkr_get`/`mkr_list` call in
  `reviewrecord.sh`) already substantively covers this without needing a near-duplicate test, since
  the new ancestor-check reads no config key at all. Reasonable minimal choice, not a real gap.

## Verification discipline

Both reviewers hand-traced all six new fixtures (`TC-RRF-21` through `TC-RRF-26`) against the real,
current `.claude/hooks/lib/reviewrecord.sh` logic and real git DAG semantics — not the commit
message's own claims — including `TC-RRF-26`'s full AD-2/AD-3-then-docs-chain recursion chain
reaching a genuine non-equal ancestor. `mkr-security-reviewer` additionally traced the trust
boundary for `expected_prior_tip` against both real callers (`mkr-gate.yml`, confirming it's forced
empty on `pull_request` and sourced from `github.event.before` on `push`; `pre-push-review-guard.sh`,
sourced from git's own pre-push protocol) and confirmed neither lets a pusher supply their own
value, and re-verified `TC-MRF-03/04/05`'s existing adversarial shapes are unaffected by the new
check's ordering. `mkr-code-reviewer` additionally re-verified `docs/adr/0008`'s Decision section
was left untouched (cross-referenced, not rewritten) and that `MKR_ADR_DIR`'s real `config.sh`
default matches what `TC-RRF-26`'s fixture assumes.

Full suite (`config_test.sh` 120/120, `mkr_artifact_test.sh` 245/245, `hooks_test.sh` 180/180,
`install_test.sh` 74/74) green before this record, re-confirmed after the `--` fix.

## Verdict

VERDICT: READY
