# Code review — adf5036 (fix(hooks): find_review_record recognizes MKR_AUDITS_DIR as an allowed non-code hop)

**Reviewers.** Two independent fresh agents, `mkr-code-reviewer` and `mkr-security-reviewer`,
spawned in parallel against this commit's diff, neither aware of the other's findings until each
had formed its own judgment.

mkr-code-reviewer: READY
mkr-security-reviewer: READY

**Scope.** `.claude/hooks/lib/reviewrecord.sh` (widens `find_review_record`'s docs-only fallback
allowed-path set to include `MKR_AUDITS_DIR`), `.claude/skills/mkr-audit/SKILL.md` (new "commit
the record alone, in its own commit, touching nothing else" instruction), `.claude/skills/
mkr-code-review/SKILL.md` and `.claude/skills/mkr-merge/SKILL.md` (allowed-path prose kept
accurate), `.github/workflows/mkr-gate.yml` (comment only, no logic change), `docs/adr/
0009-review-record-audit-path-fallback.md` (new), `tests/hooks_test.sh` (new TC-RRF-17/18),
`tests/mkr_artifact_test.sh` (new TC-RRF-19/20, widened TC-RRF-14), `specs/
ReviewRecordAuditPathFallback_Spec.md` (new), and `tests/config_test.sh` (unrelated: `TC-06c`
skipped under root EUID, a pre-existing environment limitation confirmed identical on the
unmodified base branch, called out separately in the spec's §3 and this commit's own message).

## Findings

None blocking from either reviewer.

Non-blocking, not actioned (left for a future touch, per each reviewer's own framing):
- `mkr-code-reviewer`: no dedicated test for a chain mixing an ADR commit and an audit commit
  together — the logic is sound by inspection (independent per-file/per-hop checks), not a defect.
- `mkr-security-reviewer`: same gap, independently flagged — no test for `MKR_AUDITS_DIR`
  explicitly configured empty/malformed (already guarded by the existing `[ -n "$audits_dir" ]`
  check, traced by hand to fail closed, not open).
- `mkr-code-reviewer`'s one actionable note — `mkr-code-review/SKILL.md`'s own copy of the
  allowed-path prose was stale even before this diff (missed `MKR_ADR_DIR` after `docs/adr/0008`)
  — **fixed in this same commit** rather than left open, since it was a one-line, same-class
  correction already being made elsewhere in this diff.

## Verification discipline

Both reviewers hand-traced `find_review_record`'s recursion against the new `TC-RRF-17`/`TC-RRF-18`
fixtures rather than trusting the diff's own claims; `mkr-security-reviewer` additionally traced
the case-arm's quoting against an adversarial `MKR_AUDITS_DIR` config value (glob metacharacters,
empty string) and confirmed a malicious `.mkr/config` edit can't retroactively widen the allowed
set for an ancestor commit, since editing `.mkr/config` itself trips the outside-check at that hop.
Full suite (`config_test.sh` 120/120, `mkr_artifact_test.sh` 243/243, `hooks_test.sh` 174/174,
`install_test.sh` 74/74) green before this commit.

## Verdict

VERDICT: READY
