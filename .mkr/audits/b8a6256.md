# Grounding audit — ReviewRecordPullRequestPriorTip, commit b8a6256

Fresh-context agent, no memory of the build, per DESIGN.md phase 9. Audited against
`specs/ReviewRecordPullRequestPriorTip_Spec.md` §10 AC-1…AC-6, branch `main`.

| AC | Verdict | Evidence |
|---|---|---|
| AC-1 | VERIFIED | `mkr-gate.yml`'s `EXPECTED_PRIOR_TIP` line confirmed to read `${{ github.event.before \|\| github.event.pull_request.base.sha \|\| '' }}`; hand-diffed against the pre-fix commit, which had no `pull_request.base.sha` term at all. |
| AC-2 | VERIFIED | The step's comment now states both cases explicitly. Independently reproduced `TC-GATE-02`'s own discriminator by hand against a separately-pulled old/new file pair: the stale "never expected to itself be a merge commit" clause is present pre-fix, absent post-fix. |
| AC-3 | VERIFIED | Fresh runs, all four suites: `config_test.sh` 120/0, `mkr_artifact_test.sh` 249/0 (including explicit `ok` for TC-GATE-01..04), `hooks_test.sh` 180/0, `install_test.sh` 74/0. Also independently re-ran TC-GATE-01/02's own grep logic by hand against hand-pulled old/new file copies, confirming true-positive/false-positive discrimination, not just trusting the harness. |
| AC-4 | VERIFIED | `git diff d2c2701..b8a6256 -- reviewrecord.sh` shows exactly one added comment line; signature byte-identical; no `config.sh`/`.mkr/config` key added. |
| AC-5 | VERIFIED | `docs/adr/0011-review-record-pull-request-prior-tip.md` exists, Accepted, contains the full trust-equivalence argument. Independently re-ran `TC-GATE-03`'s grep logic by hand against the file. |
| AC-6 | NOT VERIFIED (live-CI-dependent) | This criterion requires observing this fix's own grounding-audit-record PR resolve on its **own** `pull_request`-triggered check — not observable from a local audit. `f57afe9.md` did not yet exist under `.mkr/audits/` at the time of this audit. Disclosed as outstanding rather than fabricated; this exact PR (landing both this record and `f57afe9.md`'s) is the live proof and its own CI result closes this criterion the moment it resolves. |

**Additional checks the auditor ran independently:**
- Confirmed `.mkr/reviews/22b4b31.md` (the G4 record for this fix) exists, both reviewers READY,
  including the security reviewer's own constructed-and-disproved exploit attempt against the
  newly-enabled AD-2/AD-3 path on `pull_request` events.
- YAML-parsed the resulting `mkr-gate.yml` to confirm it's syntactically well-formed.

**Verdict:** FAIL (1 not verified — AC-6, resolved live by this commit's own CI run)
