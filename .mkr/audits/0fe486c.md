# Grounding audit — WorktreePermissionScope, commit 0fe486c

Fresh-context agent, no memory of the build, per DESIGN.md phase 9. Audited against
specs/WorktreePermissionScope_Spec.md §10 AC1…AC4, branch `worktree-worktree-permission-scope-docs`
(merged via PR #29).

| AC | Verdict | Evidence |
|---|---|---|
| AC1 | VERIFIED | `README.md`'s new `## Worktrees` section (lines 98–102), positioned between `## Installing` and `## Build status`, states `MKR_WORKTREE_POLICY=enforced` requires a real, registered worktree and links `specs/WorktreeGuard_Spec.md`. |
| AC2 | VERIFIED | Same section (lines 104–108) states the permission-allow-list gotcha and the `EnterWorktree`-first workaround, matching the criterion's required content. |
| AC3 | VERIFIED | The section states only the outcome plus the link — no restatement of either guard's registration/collision-check mechanics or AD-1..AD-5. `specs/WorktreeGuard_Spec.md` confirmed real, `ACCEPTED rev 11`, not a dangling link. |
| AC4 | NOT VERIFIED (literal wording contradicted by the actual branch diff) | `git diff --stat bec8d53 0fe486c^2` (the real branch-vs-branch-point diff) shows three files, not one: `README.md`, `specs/WorktreePermissionScope_Spec.md`, `.mkr/reviews/eb3aa12.md`. AC4 literally says "only README.md changed" — the spec's own DoD checkbox only verified this against the single implementing commit in isolation, not the full branch diff AC4 actually specifies. The underlying intent (§3/`TC-WPS-03`: no code/hook/config file touched) genuinely holds — the two extra files are ordinary spec-first-workflow artifacts (the spec document and its G4 review record), not a code-level workaround. Already flagged non-blocking by `mkr-code-reviewer` in `.mkr/reviews/eb3aa12.md` and never fixed before merge — a known, pre-disclosed gap, not a new one. |

**Additional checks the auditor ran independently:**
- Full `MKR_TEST` re-run fresh: `config_test.sh` 123/123, `mkr_artifact_test.sh` 249/249,
  `hooks_test.sh` 180/180, `install_test.sh` 74/74 — matching the DoD's claimed counts.
- Confirmed the §3/§11 dependency claim directly: `specs/WorktreeGuard_Spec.md` exists on `main` at
  this commit, `Status: ACCEPTED rev 11 (kikrgbh, 2026-08-11)`.
- Confirmed `.mkr/reviews/eb3aa12.md` exists, both G4 reviewers independently `READY`, and its
  content pre-discloses the AC4 imprecision above rather than being silent on it.
- Confirmed `EnterWorktree` is a real, already-exercised mechanism (referenced in
  `tests/hooks_test.sh`), not an invented tool name.
- `.mkr/audit.jsonl` does not exist in this checkout (gitignored, never committed) — genuinely
  unverifiable corroboration of the original session's tool-call trail; disclosed, not treated as
  pass or fail either way.

**Outstanding, not a defect:**
- None beyond AC4 itself.

**Verdict:** FAIL (1 not verified)
