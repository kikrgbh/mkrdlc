# Legacy orphan

> This doc exists on disk but is deliberately NOT in this fixture's `MKR_RKP_TOPICS` list. Used to
> manually verify specs/RkpTopicShape_Spec.md §7's present-but-undeclared-file rule: a full-package
> refresh run against this fixture must leave this file untouched and never report it (not as
> missing, not as orphaned, not as "no longer applicable").
